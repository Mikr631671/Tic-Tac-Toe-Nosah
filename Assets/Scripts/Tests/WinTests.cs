using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class WinTests
{
    //private GameBoard _gameBoard;

    [SetUp]
    public void SetUp()
    {
        //_gameBoard = new GameBoard();
    }

    [Test]
    public void Win_Should_Return_True_For_Winning_Combination()
    {
        // Set up a winning combination on the board
        //_gameBoard.SetCell(0, 0, PlayerSymbolType.Cross);
        //_gameBoard.SetCell(0, 1, PlayerSymbolType.Cross);
        //_gameBoard.SetCell(0, 2, PlayerSymbolType.Cross);

        // Ensure CheckWinCondition returns true for the winning combination
        //Assert.IsTrue(_gameBoard.CheckWinCondition(PlayerSymbolType.Cross));
    }
}
